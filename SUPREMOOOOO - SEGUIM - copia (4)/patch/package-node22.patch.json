{
  "devDependencies": {
    "vite": "^5.4.10",
    "esbuild": "^0.21.5",
    "@vitejs/plugin-react": "^4.3.1"
  },
  "overrides": {
    "esbuild": "^0.21.5"
  },
  "resolutions": {
    "esbuild": "^0.21.5"
  },
  "scripts": {
    "versions": "node -p \"process.version\" && npx vite -v && node -e \"console.log('esbuild', require('esbuild').version)\""
  }
}